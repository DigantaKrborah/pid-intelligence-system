# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12

# ── Data Validation ────────────────────────────────────────────────────────────
pydantic==2.10.3
pydantic-settings==2.7.0

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0          # async PostgreSQL driver (async routes)
psycopg2-binary==2.9.10  # sync PostgreSQL driver (agent tools)
alembic==1.14.0          # migrations

# ── Vector DB ─────────────────────────────────────────────────────────────────
chromadb==0.5.23

# ── Graph ─────────────────────────────────────────────────────────────────────
networkx==3.4.2

# ── LLM / Agents ──────────────────────────────────────────────────────────────
langchain==0.3.13
langchain-community==0.3.13
langchain-ollama==0.2.3
langchain-google-genai==2.0.7
google-generativeai==0.8.3

# ── PDF / Vision ──────────────────────────────────────────────────────────────
pdf2image==1.17.0        # PDF page → image (requires poppler)
pypdf==5.1.0             # PDF metadata + text extraction
Pillow==11.0.0
opencv-python-headless==4.10.0.84
pytesseract==0.3.13      # OCR via Tesseract (reads tag text from image-only PDFs)

# ── Document Processing ────────────────────────────────────────────────────────
python-docx==1.1.2
openpyxl==3.1.5

# ── Frontend ──────────────────────────────────────────────────────────────────
streamlit==1.41.1
streamlit-agraph==0.0.45
plotly==5.24.1
pandas==2.2.3

# ── Utilities ─────────────────────────────────────────────────────────────────
httpx==0.28.1
python-dotenv==1.0.1
aiofiles==24.1.0
loguru==0.7.3
tenacity==9.0.0
PyGithub==2.5.0          # in-app bug reporting → GitHub Issues
